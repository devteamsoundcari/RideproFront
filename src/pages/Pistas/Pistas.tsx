import * as React from 'react';
import { MainLayout } from '../../components/templates';

export interface IPistasProps {}

export function Pistas(props: IPistasProps) {
  return <MainLayout>Pistas</MainLayout>;
}
